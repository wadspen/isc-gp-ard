

for task in "checker"
do
   for proc_type in "filter_gsr" #"filter_nogsr" "nofilt_gsr" "nofilt_nogsr"
   do
      for model in "gp" "glm"
      do
         for nut in "1000" #"3"
         do
            for nul in "1000" "3"
            do
             	echo "$task" "$proc_type" "$model" "$nut" "$nul"

		sbatch ./mc_fit_g.sh "$task" "$proc_type" "$model" "$nut" "$nul"
           done
         done
      done
   done
done  
